apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app.name" . }}-{{ .Values.env }}
  namespace: {{ .Values.namespace }}-{{ .Values.env }}
  labels:
  {{- include "app.labels" . | nindent 4 }}
spec:
  {{- if not .Values.ingress.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{ end }}
  selector:
    matchLabels:
      app: {{ include "app.name" . }}
      env: {{ .Values.env }}
  template:
    metadata:
      labels:
        app: {{ include "app.name" . }}
        env: {{ .Values.env }}
      {{- include "app.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - image: {{ .Values.image.name }}:{{ .Values.image.tag }}
          name: {{ .Values.containerName }}
          {{- if .Values.containerPort.exists }}
          ports:
            {{- toYaml .Values.containerPort.ports | nindent 10 }}
            {{ end }}
          {{- if .Values.livenessProbe }}
          livenessProbe:
            httpGet:
              path: {{ .Values.healthCheck }}
              port: http
          readinessProbe:
            httpGet:
              path: {{ .Values.healthCheck }}
              port: http
          {{ end }}
          resources: {{- toYaml .Values.deployment.resources | nindent 12 }}
      imagePullSecrets:
      - name: {{ include "app.name" . }}-creds-{{ .Values.env }}